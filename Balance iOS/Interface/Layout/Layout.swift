import UIKit

enum Layout {

    enum Sizes {
        
        enum Controller {
            
            static var form_sheet_size: CGSize { .init(width: 540, height: 620) }
            static var split_side_bar_preferred_width: CGFloat { 215 }
        }
    }
}
